[package]
name = "hozondb-core"
version = "0.1.0"
edition = "2024"

[dependencies]
tonic = { workspace = true}
prost = {workspace = true}

[dev-dependencies]

[build-dependencies]
tonic-build = { workspace = true}
